package com.orhrm.selenium1;

import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.firefox.FirefoxDriver;

public class quikrparentchild {

	public static void main(String[] args) {
		/*System.setProperty("webdriver.gecko.driver", ".\\ExecutableFiles\\geckodriver.exe");

		FirefoxDriver driver=new FirefoxDriver();
		driver.get("https://quikr.com/");
		driver.manage().window().maximize();
		driver.findElement(By.id("searchedCat")).click();
		List<WebElement> links= driver.findElements(By.xpath("//div[@id='category-dropdown']/ul/li[4]/a"));
		System.out.println(links.size());
		for(int i=0;i<=links.size();i++)
		{
			System.out.println(links.get(i).getText());
		}*/
	}

}
